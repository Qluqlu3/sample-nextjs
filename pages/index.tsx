import type { NextPage } from 'next';
import { Button } from 'components/Button';

const Home: NextPage = () => {
  const xxx = '';
  return (
    <div>
      <Button message={'ボタン'}/>
    </div>
  );
};

export default Home;
